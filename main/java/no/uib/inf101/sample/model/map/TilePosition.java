package no.uib.inf101.sample.model.map;

/**
* A tile in the game consists of a row and a column
* @param row is the tile's row
* @param col is the tile's column 
*/
public record TilePosition (int row, int col){} 